# Results

Runtime prediction and metric outputs are written here.

## Retained files

- `README.md`
- `model_snapshots/NousResearch_Meta-Llama-3-8B-Instruct_preds_metrics.txt`: historical summary restored from git commit `ec10c47`; the original prediction jsonl was removed during cleanup, so this text file is the retained artifact.

## Typical files

- inference `*.jsonl`
- metric reports `*.txt`
- structured metrics `*.json`
- benchmark summary tables

Most generated outputs stay ignored by default in `CODE/.gitignore`; only the retained historical snapshot above is checked in by default.
